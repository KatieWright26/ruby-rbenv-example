steps:
  - label: "Test"
    command: |
      bundle install 
      bktec run
    plugins:
      - tests#v1.0.0:
          test-runner: rspec
          result-path: tmp/rspec-result.json
          suite-slug: onboarding-flow-test
    parallelism: 2